import { useState } from 'react';

const Container = () => {
    const [activeCard, setActiveCard] = useState("cc-001");
    const [privacyMode, togglePrivacyMode] = useState(false);
    return (

    );
}

export default Container;